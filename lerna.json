{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md", "*.test.js"],
      "registry": "https://registry.npmjs.org/"
    }
  }
}
